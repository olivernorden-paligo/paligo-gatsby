/**
 * @type {import('gatsby').GatsbyConfig}
 */
module.exports = {
    siteMetadata: {
        title: `Paligo Gatsby`,
        siteUrl: `https://www.yourdomain.tld`,
    },
    plugins: [
        // 'paligo-xml-plugin'
    ],
};
